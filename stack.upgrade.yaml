user-message: "WARNING: This stack project is generated."
flags:
  mongoDB:
    _old-network: false

resolver: nightly-2025-12-30

# We have upgraded 7 packages and have 1 yet to do.
packages:
  - ./persistent
  - ./persistent-mongoDB
  - ./persistent-postgresql
  - ./persistent-qq
  - ./persistent-redis
  - ./persistent-sqlite
  - ./persistent-test

extra-deps:
  # Package versions for published packages either not on Stackage or
  # not matching the version on Stackage for the resolver we use.
  # These package-version extra dependencies are equivalent to cabal constraints.
  - mongoDB-2.7.1.4
  - resource-pool-0.4.0.0
  - cryptohash-0.11.9
  - cryptonite-0.30
  - mysql-0.2.1
  - mysql-simple-0.4.9
